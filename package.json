{
  "name": "kakuro",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node tools/build-sw.ts",
    "packs": "node tools/make-packs.ts",
    "verify": "node tools/verify-packs.ts",
    "icons": "node tools/make-icons.ts",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test tests/solver.test.ts tests/encode.test.ts tests/links.test.ts"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  }
}
